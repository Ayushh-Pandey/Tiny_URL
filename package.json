{
  "name": "tinyurl",
  "version": "1.0.0",
  "private": true,
  "description": "URL Shortener with React and Node.js",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "install:all": "cd Backend && npm install && cd ../Frontend && npm install",
    "build": "cd Frontend && npm run build"
  }
}
